module Hello = {

  type Expr =
    | EInt Int
    | EAdd Expr Expr
    | EBlock (List Stmt)

  type Stmt =
    | SExpr Expr
    | SLet Int Expr

  type List a =
    | Nil
    | Cons a (List a)

  def main = 1

}