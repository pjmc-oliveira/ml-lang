module Hello = {
  type List a = Nil | Cons a (List a)
  type Maybe a = None | Some a

  def overlapping = \ls
    match ls with
      | Nil -> None
      | Nil -> None
      | Cons h t -> None
      | Cons h t -> Some h
    end

  def missing = \ls
    match ls with
      | Nil -> None
    end

  def main = 1
}