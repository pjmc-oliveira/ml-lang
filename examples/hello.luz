module Hello = {
  def fact : Int -> Int = \x
    if eq 0 x then
      1
    else
      mul x (fact (sub x 1))

  def fib : Int -> Int = \x
    if eq 0 x then
      1
    else
      add (fib (sub x 1)) (fib (sub x 2))

  def main = fib 1
}